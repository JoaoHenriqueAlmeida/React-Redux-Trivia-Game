import React, { Component } from 'react';

export default class Config extends Component {
  render() {
    return (
      <div>
        <h1 data-testid="settings-title">Bem Vindo a Config</h1>
      </div>
    );
  }
}
